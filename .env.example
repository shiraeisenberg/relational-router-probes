# API Keys
ANTHROPIC_API_KEY=sk-ant-...        # For synthetic data generation
HF_TOKEN=hf_...                      # For gated model access (optional)

# Modal (usually handled by `modal setup`)
# MODAL_TOKEN_ID=...
# MODAL_TOKEN_SECRET=...

# Paths (optional, defaults work)
# DATA_DIR=./data
# RESULTS_DIR=./results
# CACHE_DIR=./cache
